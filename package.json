{
  "name": "backtester-v2",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run src/index.ts",
    "dev:watch": "bun --watch src/index.ts",
    "type-check": "bun tsc --noEmit",
    "type-check:watch": "bun --watch tsc --noEmit",
    "test": "bun test",
    "test:watch": "bun test --watch"
  },
  "dependencies": {
    "typescript": "^5.8.3",
    "zod": "^4.1.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "prettier": "^3.7.4"
  }
}
